# 留一點空間，給思考。

歡迎來到 **HUI**，一個安靜、輕盈的 Markdown 工作空間。

## 閱讀，自有節奏

切換原始碼、分欄、即時與閱讀模式。在排版設定中調整主題、語言、字級、行高、段落間距與版心寬度。

## 從簡單的文字開始

用 `#` 寫標題，用 **粗體**、*斜體* 和清單組織想法。

開啟本機 `.md` 檔案，變更將在停止輸入後自動儲存。文件始終留在你的裝置上。

- [x] 找到適合自己的主題
- [ ] 寫下今天的第一個想法

## 表達，不止文字

```rust
fn main() {
    println!("Hello, HUI!");
}
```

$E = mc^2$

$$
\int_0^1 x^2\,dx = \frac{1}{3}
$$
